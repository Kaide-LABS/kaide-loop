# conformance-ledger.md

> **Novelty flag:** this classification is a novel mechanism carried by its human-escalation path (CONFLICT -> Gate 3; AMBIGUOUS -> open question), not an application of validated technique. No prior art, labelled dataset, or published baseline exists for convention-vs-cruft discrimination (loopr-PRD.md section 5 A3a).

## Routine calls (applied silently, no gate)
- **conform** `decorator:pytest.mark.parametrize`: Standard, canonical pytest convention, not obscure or project-specific -- same call already made repeatedly this session. Core centrality, actively maintained, single-author 100% is the expected baseline for this solo repo, no deprecation markers, no conflict with this build's intent. New parametrized tests this build adds should follow the same convention.
- **conform** `import:__future__`: Explicitly mandated stack convention (loopr-MIGRATION.md SS8's forward-reference discipline), not just observed -- same call made repeatedly today across every touched-surface classification. Core centrality, actively maintained, single-author 100% is the expected baseline for this solo repo, no deprecation markers, no conflict with intent. Any new module this build adds must include it.
- **conform** `import:json`: Standard library import, core centrality, actively maintained, no deprecation markers, no ambiguity. Expected wherever structured logging/state serialization is needed, consistent with this build's own append-only log requirement.
- **conform** `import:loopr`: Importing loopr's own package is exactly what a thin orchestration wrapper and its tests are supposed to do -- core centrality, actively maintained, no deprecation markers, no conflict with intent. The 'weak evidence' qualifier reflects occurrence count at threshold, not doubt about legitimacy.
- **conform** `import:loopr.customization.customize`: Standard internal module import, actively maintained, no deprecation markers. Occurrence count at threshold reflects import-site count, not doubt about legitimacy -- importing the customization module where a new step-14 customize command would be added is exactly the existing convention, no conflict with intent.
- **conform** `import:loopr.customization.templates`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Importing the template-discovery module where a new step-14 template and its customization logic would live is exactly the existing convention, no conflict with intent.
- **conform** `import:loopr.errors`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Occurrence count at threshold reflects import-site count, not doubt about legitimacy -- importing loopr's shared error types where the new step-14 command needs error handling is exactly the existing convention.
- **conform** `import:loopr.judge.envelope`: Standard internal module import, actively maintained, no deprecation markers. Expected wherever the judge envelope format is needed, consistent with existing precedent this session; no conflict with intent.
- **conform** `import:loopr.models.common`: Standard internal module import, core centrality, high recurrence, actively maintained, no deprecation markers. Shared common types are expected to be widely imported wherever a new customization step is added; no conflict with intent.
- **conform** `import:loopr.models.customization`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Occurrence count at threshold reflects import-site count, not doubt about legitimacy -- importing the customization models module wherever new step-14 customization state is defined is exactly the existing convention.
- **conform** `import:loopr.models.interrogation`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Interrogation-state types are expected to be widely imported wherever the confirmed baby PRD or state file is read; no conflict with intent.
- **conform** `import:loopr.models.judge`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Judge envelope types are expected to be widely imported wherever a new customization step's judge/fidelity calls are defined; no conflict with intent.
- **conform** `import:loopr.state.store`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Threshold-level occurrence count reflects import-site count, not doubt about legitimacy -- reading/writing state via the StateStore is exactly the existing convention wherever a --state file is touched, no conflict with intent.
- **conform** `import:pathlib`: Standard library import, core centrality, high recurrence, actively maintained, no deprecation markers, no ambiguity. Expected wherever file paths are handled, consistent with intent.
- **conform** `import:pytest`: Standard testing framework import, universally recognized. Occurrence count at threshold reflects import-site count, not doubt about legitimacy. Actively maintained, no deprecation markers, no conflict with intent.
- **conform** `import:sys`: Standard library import, core centrality, actively maintained, no deprecation markers, no ambiguity. Expected wherever stderr output/exit codes are handled, consistent with the CLI's existing conventions.
