# conformance-ledger.md

> **Novelty flag:** this classification is a novel mechanism carried by its human-escalation path (CONFLICT -> Gate 3; AMBIGUOUS -> open question), not an application of validated technique. No prior art, labelled dataset, or published baseline exists for convention-vs-cruft discrimination (loopr-PRD.md section 5 A3a).

## Routine calls (applied silently, no gate)
- **conform** `decorator:dataclass`: Core, recently touched, no deprecation markers, and does not conflict with the stated intent -- the new vagueness-check code will itself want a plain dataclass result type (matching StructuralResult's own shape), so following this convention is correct, not cruft. The single-author signal is expected for a young, single-author project and isn't itself a negative signal here. occurrence_count is at the minimum threshold (weak on its own per the qualifier), but centrality=core and the absence of any conflicting signal make CONFORM the right call, not AMBIGUOUS.
- **conform** `decorator:model_validator`: model_validator backs loopr's own load-bearing invariants (two-layer check_two_layer_invariant, check_mode_coherent, check_confirmation_coherent, check_resolution_coherent) -- this is deliberate architecture, not an incidental pattern. Peripheral centrality here just reflects that this specific change doesn't need a new validator, not that the convention itself is marginal. No conflict with the stated intent: nothing about the new vagueness check requires bypassing or duplicating this pattern.
- **conform** `import:__future__`: A universal, module-wide boilerplate convention (postponed annotation evaluation) applied consistently across every file in the touched surface, including tests. No deprecation, core centrality, no conflict with the stated intent -- new modules in this change should carry it too.
- **conform** `import:dataclasses`: Standard-library import used consistently wherever dataclasses appear in this codebase's own convention (paired with the @dataclass decorator pattern already classified CONFORM). Core, recent, no deprecation, no conflict with the stated intent.
- **conform** `import:loopr.judge.envelope`: Core internal module import (digest/call_id helpers) used consistently by both the structural checks and their tests. No conflict with the stated intent; new code touching this surface should use the same shared utility rather than reimplementing it.
- **conform** `import:loopr.models.common`: Shared enums/base-model import used across nearly every module and test in the touched surface. Core, recent, no conflict -- new code will need these enums too.
- **conform** `import:loopr.models.interrogation`: Import of the interrogation-state models the new structural check will read acceptance_criteria from directly. Core, recent, no conflict.
- **conform** `import:loopr.models.judge`: Import of JudgeRequest/JudgeResponse, needed by anything constructing or handling a judge call. Core, recent, no conflict -- the new C2 keyword check itself doesn't need this directly, but conditions.py's own orchestration does.
