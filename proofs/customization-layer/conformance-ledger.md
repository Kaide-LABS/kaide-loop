# conformance-ledger.md

> **Novelty flag:** this classification is a novel mechanism carried by its human-escalation path (CONFLICT -> Gate 3; AMBIGUOUS -> open question), not an application of validated technique. No prior art, labelled dataset, or published baseline exists for convention-vs-cruft discrimination (loopr-PRD.md section 5 A3a).

## Routine calls (applied silently, no gate)
- **conform** `decorator:model_validator`: Recurring (6 occurrences, above the threshold), actively maintained (touched today, in the same commit as the boundary-proposal fix), no deprecation markers, single-author/100%-commit-share is the expected baseline for this solo-founder repo rather than a cruft signal. This is Pydantic's own standard validator decorator, already used throughout the codebase for exactly this shape of work (check_mode_coherent, check_scope, check_response_shape). Does not conflict with the stated intent -- any new validation logic this build's models need should follow the same pattern, not invent an alternative.
- **conform** `decorator:pytest.fixture`: pytest.fixture is a standard, universally-recognized Python testing convention, not an obscure or project-specific one -- the 'weak evidence of intentionality' qualifier reflects the occurrence count sitting exactly at the recurrence threshold, not genuine doubt about whether this pattern is legitimate. Actively maintained (touched 4 days ago), no deprecation markers, doesn't conflict with the build's intent. Any new test fixtures this build adds should use the same decorator, consistent with the existing suite.
- **conform** `decorator:pytest.mark.parametrize`: pytest.mark.parametrize is a standard, canonical pytest convention for parametrized test cases -- not obscure or project-specific. The 'weak evidence of intentionality' qualifier reflects the occurrence count sitting at the recurrence threshold, not doubt about the pattern's legitimacy. Actively maintained (4 days ago), no deprecation markers, no conflict with the build's intent. New parametrized tests this build adds should follow the same convention.
- **conform** `import:__future__`: This is not just an observed pattern but an explicitly mandated stack convention -- loopr-MIGRATION.md SS8's forward-reference discipline requires it. Recurring across 12 locations, core centrality, actively maintained (touched today), no deprecation markers. Any new modules this build adds must include it, matching the rest of the codebase.
- **conform** `import:json`: Standard library import, core centrality, actively maintained, no deprecation markers, no conflict with the build's intent. Nothing to avoid here -- any new code needing JSON handling should use it the same way.
- **conform** `import:loopr`: Importing loopr's own package is exactly what a thin orchestration wrapper and its tests are supposed to do -- core centrality, actively maintained, no deprecation markers, no conflict with the build's intent.
- **conform** `import:loopr.gates.gates`: Standard internal module import, actively maintained, no deprecation markers. Occurrence count at threshold reflects import-site count, not doubt about legitimacy -- importing gates.gates where gate logic is needed is expected, not cruft.
- **conform** `import:loopr.judge.envelope`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Expected wherever the judge envelope format is needed.
- **conform** `import:loopr.models.brownfield`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Expected wherever brownfield models are needed.
- **conform** `import:loopr.models.common`: Standard internal module import, high recurrence (12), core centrality, actively maintained, no deprecation markers. Shared common types are expected to be widely imported.
- **conform** `import:loopr.models.gates`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Threshold-level occurrence count reflects import-site count, not doubt about legitimacy.
- **conform** `import:loopr.models.interrogation`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Interrogation-state types are expected to be widely imported.
- **conform** `import:loopr.models.judge`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Judge envelope types are expected to be widely imported.
- **conform** `import:loopr.state.store`: Standard internal module import, core centrality, actively maintained, no deprecation markers. Threshold-level occurrence count reflects import-site count, not doubt about legitimacy.
- **conform** `import:pathlib`: Standard library import, core centrality, actively maintained, no deprecation markers, no ambiguity.
- **conform** `import:pydantic`: Pydantic is loopr's one mandated runtime dependency (loopr-MIGRATION.md SS8), core centrality, actively maintained, no deprecation markers. Unambiguous.
- **conform** `import:subprocess`: Standard library import, core centrality, actively maintained, no deprecation markers. Plausibly how the wrapper invokes the loopr CLI as an external process -- consistent with this build's own confirmed shape (a thin orchestration layer driving the existing CLI contract, not reimplementing it in-process).
